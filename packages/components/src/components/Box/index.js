import styled from 'styled-components/native';
import {
  alignSelf,
  alignItems,
  alignContent,
  justifyContent,
  width,
  height,
  flex,
  borderColor,
  left,
  right,
  top,
  bottom,
  space,
  borderBottom,
  borderRadius,
  borderLeft,
  borderRight,
  borderWidth,
  borderTop,
  display,
  flexDirection,
} from 'styled-system';

const Box = styled.View`
  ${alignSelf}
  ${alignItems}
  ${alignContent}
  ${justifyContent}
  ${width}
  ${height}
  ${flex}
  ${borderColor}
  ${left}
  ${right}
  ${top}
  ${bottom}
  ${space}
  ${borderBottom}
  ${borderRadius}
  ${borderLeft}
  ${borderRight}
  ${borderWidth}
  ${borderTop}
  ${display}
  ${flexDirection}
`;

export default Box;
